# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## プロジェクト概要

realestate_app — 不動産関連のアプリケーション。現時点ではプロジェクトは未着手で、コードベースは存在しない。技術スタック・アーキテクチャ・ビルド/テストコマンドは未確定のため、それらが定まり次第このファイルを更新すること。

## Git 運用ルール

- **コードを変更するたびに GitHub へプッシュする**。編集 → コミット → `git push` を1サイクルとして扱い、変更を手元に溜めない。
- コミットはまとまった単位で作成する（1行の修正でもコミット＆プッシュして良い。半端な状態のまま放置しないことを優先する）。
- コミットメッセージは「何を」ではなく「なぜ」を中心に簡潔に書く。
- `git push --force` などの破壊的操作はユーザーから明示的に指示があった場合のみ実行する。
- リモートが未設定の場合は、プッシュ前にユーザーに確認する。

## このファイルの更新について

技術選定・ディレクトリ構成・主要コマンドが決まったタイミングで、以下の節を追記すること：

- **Commands**: ビルド / lint / テスト / 単一テスト実行 / 開発サーバ起動 などの実コマンド
- **Architecture**: 複数ファイルを横断して理解する必要のある「大きな構造」（モジュール境界、データフロー、外部サービス連携など）

具体や個別ファイルの羅列ではなく、初見の Claude が素早く生産的になるための最小限の情報に絞ること。
